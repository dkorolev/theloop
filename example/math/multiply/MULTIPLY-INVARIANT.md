# Multiply Invariant

This directory must contain exactly three files: `a.txt`, `b.txt`, and `c.txt`. Each file must contain a single number. The value in `c.txt` must equal the product of the value in `a.txt` and the value in `b.txt`.
